<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Impressão</title>
    <style>
      /* Estilos gerais para tela e impressão */
      body {
        font-family: 'Times New Roman', serif;
        font-size: 14pt;
        margin: 30px;
      }

      .promissory-wrapper {
        width: 48%;
        border: 1px solid #000;
        padding: 15px;
        margin: 1%;
        box-sizing: border-box;
        display: inline-block;
        vertical-align: top;
        height: 350px;
        /* Evita quebra no meio da nota */
        page-break-inside: avoid;
      }

      .signature {
        margin-top: 40px;
        text-align: center;
      }

      .signature-line {
        margin-top: 20px;
        border-top: 1px solid #000;
        width: 70%;
        margin-left: auto;
        margin-right: auto;
      }

      .page-break {
        page-break-after: always;
      }

      /* Configurações específicas para impressão */
      @page {
        size: A4;
        margin: 15mm 10mm 10mm 10mm;
      }

      @media print {
        body {
          margin: 0;
          padding: 0;
          font-family: 'Times New Roman', serif;
          font-size: 14pt;
        }

        /* Esconde elementos que não devem aparecer na impressão */
        .no-print {
          display: none !important;
        }

        /* Certifica que .page-break funciona no print */
        .page-break {
          display: block;
          page-break-before: always;
        }

        /* Para garantir alinhamento das notas */
        .promissory-wrapper {
          box-shadow: none; /* remove sombras se tiver */
          break-inside: avoid;
          -webkit-print-color-adjust: exact; /* para manter cores se usar */
          -moz-print-color-adjust: exact;
          print-color-adjust: exact;
        }
      }
    </style>
  </head>
  <body>
    <%= yield %>
  </body>
</html>
