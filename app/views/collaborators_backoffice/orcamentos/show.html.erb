<div class="card mt-3">
  <div class="card-header bg-primary text-white d-flex justify-content-between align-items-center">
    <h4 class="mb-0" style="text-align: center;">Orçamento #<%= @orcamento.cod_orcamento %></h4>
    <!-- VOLTAR -->
    <%= link_to collaborators_backoffice_orcamentos_path,
      class: 'btn btn-secondary btn-sm btn-acao-footer',
      title: 'Voltar' do %>
      <i class="fa fa-arrow-left me-2"></i>
    <% end %>
  </div>
  <div class="card-body">
    <div class="row my-2">
      <div class="col-md-6">
        <div class="form-floating">
          <input type="text" class="form-control border-top-0 border-start-0 border-end-0 border-dark" value="<%= @orcamento.pessoa.nome %>" readonly>
          <label>Cliente</label>
        </div>
      </div>
      <div class="col-md-3">
        <div class="form-floating">
          <input type="text" class="form-control border-top-0 border-start-0 border-end-0 border-dark" value="<%= @orcamento.funcionario.usuario %>" readonly>
          <label>Vendedor</label>
        </div>
      </div>
      <div class="col-md-3">
        <div class="form-floating">
          <input type="text" class="form-control border-top-0 border-start-0 border-end-0 border-dark" value="<%= case @orcamento.status
            when 'pendente' then 'Pendente'
            when 'aprovado' then 'Aprovado'
            when 'convertido' then 'Convertido'
            when 'rejeitado' then 'Rejeitado'
            end %>" readonly>
          <label>Status</label>
        </div>
      </div>
    </div>
    <div class="row my-2">
      <div class="col-md-3">
        <div class="form-floating">
          <input type="text" class="form-control border-top-0 border-start-0 border-end-0 border-dark" value="<%= l(@orcamento.data_orcamento, format: :short) %>" readonly>
          <label>Data Orçamento</label>
        </div>
      </div>
      <div class="col-md-3">
        <div class="form-floating">
          <input type="text" class="form-control border-top-0 border-start-0 border-end-0 border-dark" value="<%= @orcamento.data_validade ? l(@orcamento.data_validade) : 'Não definida' %>" readonly>
          <label>Validade</label>
        </div>
      </div>
      <% if @orcamento.cod_venda.present? %>
        <div class="col-md-3">
          <div class="form-floating">
            <input type="text" class="form-control border-top-0 border-start-0 border-end-0 border-dark" value="#<%= @orcamento.cod_venda %>" readonly>
            <label>Venda</label>
          </div>
        </div>
      <% end %>
    </div>
    <% if @orcamento.observacoes.present? %>
      <div class="row my-2">
        <div class="col-md-12">
          <div class="form-floating">
            <textarea class="form-control border-top-0 border-start-0 border-end-0 border-dark" readonly style="height: 60px"><%= @orcamento.observacoes %></textarea>
            <label>Observações</label>
          </div>
        </div>
      </div>
    <% end %>
    <h5 class="mt-4 mb-3">Itens do Orçamento</h5>
    <div class="dataTable-loading sortable searchable fixed-columns">
      <table id="report-budget" class="table table-striped table-hover table-bordered table-sm">
        <thead class="table-primary">
          <tr>
            <th style="width: 5%;">Codigo</th>
            <th style="width: 30%;">Produto</th>
            <th>Cor</th>
            <th style=";">Qtd</th>
            <th style=";">Valor Unit.</th>
            <th style=";">Acréscimo</th>
            <th style=";">Desconto</th>
            <th style=";">Total</th>
            <th style=";">Estoque</th>
          </tr>
        </thead>
        <tbody>
          <% @itens.each do |item| %>
            <tr>
              <td><%= item.full_codigo %></td>
              <td><%= item.produto.nome %></td>
              <td><%= item.cor&.nmcor || '-' %></td>
              <td><%= number_with_precision(item.quantidade, precision: 0) %></td>
              <td><%= "R$ #{number_to_currency(item.valorunitario, separator: ",", delimiter: "", unit:"")}" %></td>
              <td><%= "R$ #{number_to_currency(item.valor_acrescimo, separator: ",", delimiter: "", unit:"")}" %></td>
              <td><%= "R$ #{number_to_currency(item.valor_desconto, separator: ",", delimiter: "", unit:"")}" %></td>
              <td><%= "R$ #{number_to_currency(item.valor_total, separator: ",", delimiter: "", unit:"")}" %></td>
              <td>
                <% estoque = item.estoque_disponivel %>
                <span class="<%= estoque >= item.quantidade ? 'text-success' : 'text-danger' %>">
                  <%= number_with_precision(estoque, precision: 0) %>
                </span>
              </td>
            </tr>
          <% end %>
        </tbody>
        <tfoot>
          <% subtotal = @itens.sum(&:valor_total) %>
          <tr>
            <th colspan="6" class="text-end">Subtotal:</th>
            <th colspan="2" class="text-end">
              <%= number_to_currency(subtotal) %>
            </th>
          </tr>
          <% if @orcamento.desconto > 0 %>
            <tr>
              <th colspan="6" class="text-end">Desconto:</th>
              <th colspan="2" class="text-end text-danger">- <%= number_to_currency(@orcamento.desconto) %></th>
            </tr>
          <% end %>
          <% if @orcamento.acrescimo > 0 %>
            <tr>
              <th colspan="6" class="text-end">Acréscimo:</th>
              <th colspan="2" class="text-end text-success">+ <%= number_to_currency(@orcamento.acrescimo) %></th>
            </tr>
          <% end %>
          <tr>
            <th colspan="6" class="text-end">Total:</th>
            <th colspan="2" class="text-end"><%= number_to_currency(@orcamento.valortotal) %></th>
          </tr>
        </tfoot>
      </table>
    </div>
    <div class="card border-info mt-4">
      <div class="card-header bg-info-subtle text-dark">
        <strong>Analytics do orçamento público</strong>
      </div>
      <div class="card-body">
        <div class="row g-3">
          <div class="col-md-3">
            <div class="border rounded p-3 h-100">
              <div class="text-muted small">Visualizações</div>
              <div class="fs-4 fw-bold mt-2"><%= @orcamento.views_count.to_i %></div>
            </div>
          </div>
          <div class="col-md-3">
            <div class="border rounded p-3 h-100">
              <div class="text-muted small">Tempo total</div>
              <div class="fs-4 fw-bold mt-2"><%= OrcamentoAnalyticsService.format_duration(@orcamento.total_view_seconds.to_i) %></div>
            </div>
          </div>
          <div class="col-md-3">
            <div class="border rounded p-3 h-100">
              <div class="text-muted small">Primeira visualização</div>
              <div class="mt-2"><%= @orcamento.first_viewed_at ? l(@orcamento.first_viewed_at, format: :short) : '—' %></div>
            </div>
          </div>
          <div class="col-md-3">
            <div class="border rounded p-3 h-100">
              <div class="text-muted small">Última visualização</div>
              <div class="mt-2"><%= @orcamento.last_viewed_at ? l(@orcamento.last_viewed_at, format: :short) : '—' %></div>
            </div>
          </div>
        </div>
      </div>
    </div>
    <!-- RODAPÉ DE AÇÕES -->
    <% cliente_link = orcamento_publico_url(@orcamento, nome_cliente: @orcamento.pessoa.nome.to_s.parameterize) %>
    <div class="alert alert-info mt-4">
      <div class="d-flex justify-content-between align-items-center flex-wrap gap-3">
        <div>
          <strong>Link do cliente:</strong>
          <div class="mt-1"><%= cliente_link %></div>
        </div>
        <div class="text-end">
          <strong>Proteção:</strong>
          <div class="fw-bold">Últimos 4 dígitos do celular</div>
        </div>
      </div>
    </div>
    <div class="acoes-footer mt-5 d-flex justify-content-end gap-3">
      <!-- IMPRIMIR -->
      <%= link_to '#',
      onclick: 'printOrcamento(); return false;',
      class: 'btn btn-outline-secondary btn-md btn-acao-footer',
      title: 'Imprimir' do %>
        <i class="fa fa-print me-2"></i> Imprimir
      <% end %>
      <!-- CONVERTER EM VENDA -->
      <% if @orcamento.pode_converter? %>
        <%= link_to converter_venda_collaborators_backoffice_orcamento_path(@orcamento),
        method: :post,
        data: { confirm: 'Deseja converter este orçamento em venda?' },
        class: 'btn btn-success btn-md btn-acao-footer',
        title: 'Converter em venda' do %>
          <i class="fa fa-check me-2"></i> Converter em venda
        <% end %>
      <% end %>
      <!-- EDITOR VISUAL -->
      <%= link_to collaborators_backoffice_orcamento_editor_path(@orcamento),
      class: 'btn btn-primary btn-md btn-acao-footer',
      title: 'Editor Visual' do %>
        <i class="fa fa-palette me-2"></i> Editor Visual
      <% end %>
      <!-- EDITAR -->
      <% unless @orcamento.convertido? %>
        <%= link_to edit_collaborators_backoffice_orcamento_path(@orcamento),
        class: 'btn btn-warning btn-md btn-acao-footer',
        title: 'Editar' do %>
          <i class="fa fa-edit me-2"></i> Editar
        <% end %>
      <% end %>
      <!-- VOLTAR -->
      <%= link_to collaborators_backoffice_orcamentos_path,
      class: 'btn btn-secondary btn-md btn-acao-footer',
      title: 'Voltar' do %>
        <i class="fa fa-arrow-left me-2"></i> Voltar
      <% end %>
    </div>
    <!-- IFRAME DE IMPRESSÃO -->
    <iframe id="print-frame" style="display:none;"></iframe>
    <script>
      function printOrcamento() {
        const iframe = document.getElementById('print-frame');
        iframe.src = "<%= print_collaborators_backoffice_orcamento_path(@orcamento) %>";

        iframe.onload = function() {
          iframe.contentWindow.print();
        };
      }
    </script>
  </div>
</div>
<script>
  document.addEventListener('DOMContentLoaded', function() {
    $('#report-budget').DataTable({
      "paging": false,  // Desabilita a paginação
      "searching": false, // Permite a pesquisa na tabela (opcional)
      "ordering": true,  // Habilita ordenação nas colunas (opcional)
      "info": false}); // Habilita ordenação nas colunas (opcional)
  });
</script>
<style>
  .btn-acao-footer {
    padding: 12px 22px;
    font-weight: 600;
    border-radius: 8px;
  }

  .btn-acao-footer i {
    font-size: 1.1rem;
  }
  @media print {
    .acoes-footer {
      display: none !important;
    }
  }
</style>